/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true,
  },
  assetPrefix: "/OdayTarek-portfolio",
  basePath: "/OdayTarek-portfolio",
};

export default nextConfig;
